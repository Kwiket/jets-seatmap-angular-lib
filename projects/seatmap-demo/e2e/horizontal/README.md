# horizontal

Renders the seatmap rotated 90° clockwise when true.

| name  | value | expected                                       |
|-------|-------|------------------------------------------------|
| true  | true  | Plane runs left → right; rows stacked vertically. |
| false | false | Plane runs top → bottom (default vertical layout). |
