# singleDeckMode

When true, shows one deck at a time with a deck-selector tab strip.
When false, stacks all decks vertically on the same page.

`builtInDeckSelector: true` is forced so the selector remains visible in
both variants for visual comparison.

The `false` variant produces two screenshots (`-top` and `-bottom`) because
the all-decks layout overflows the viewport.
